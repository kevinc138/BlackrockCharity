<body>

  <p id="notice"><%= notice %></p>
  <% if current_user.try(:admin?) %>
  <h1>All Charities</h1>

  <%= form_for(@compare) do |f| %>
  <% if @compare.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@compare.errors.count, "error") %> prohibited this compare from being saved:</h2>

      <ul>
      <% @compare.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="form-group">
    <%= f.label :charity %><br>
    <%= f.collection_select :charity, Charity.all, :name,:name, {prompt: "Select a charity"}, :required =>true, :class => "form-control" %>
  </div>
  <div class="form-group">
    <%= f.label :charity %><br>
    <%= f.collection_select :charity, Charity.all, :name,:name, {prompt: "Select a charity"}, :required =>true, :class => "form-control" %>
  </div>
  <div class="form-group">
    <%= f.label :charity %><br>
    <%= f.collection_select :charity, Charity.all, :name,:name, {prompt: "Select a charity"}, :required =>true, :class => "form-control" %>
  </div>
  <div class="actions">
    <%= f.submit "Submit", :class => "btn btn-default btn-sm" %>
  </div>
<% end %>
</body>


