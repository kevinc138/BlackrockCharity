<body>

  <p id="notice"><%= notice %></p>
  <% if current_user.try(:admin?) %>
  <h1>All Charities</h1>

  <table class = "table table-hover">
    <thead>
      <tr>
        <th>Name</th>
        <th>Description</th>
        <th>Total</th>
        <th colspan="3"></th>
      </tr>
    </thead>

    <tbody>
      <% @charities.each do |charity| %>
        <tr>
          <td><%= charity.name %></td>
          <td><%= charity.description %></td>
          <td><%= charity.total %></td>
          <td><%= button_to 'View Details', charity %></td>
          <td><%= button_to 'Edit Details', edit_charity_path(charity) %></td>
          <td><%= button_to 'Delete', charity, method: :delete, data: { confirm: 'Are you sure?' } %></td>
        </tr>
      <% end %>
    </tbody>
  </table>

  <br>

  <%= button_to 'Add New Charity', new_charity_path %>
  <% end %>
</body>